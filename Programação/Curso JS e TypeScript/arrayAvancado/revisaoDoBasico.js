function global() {
    // // Valor por referência
    // //              0          1         2

    // // let nomes = ['Lucas', 'Eduardo', 'Maria'];

    // let novo = [...nomes];
    // let removido = nomes.pop(); // pode retorna a uma variavel

    // //console.log(nomes.length); // verifica quantos elementos dentro do array

    // nomes.shift(); // remove o primeiro elemento do array e desloca os outros indices do array

    // nomes.push('Mario') // adiciona um elemento no final do meu array
    // nomes.unshift('João') // adiciona um elemento no começo do array indice 0
    // let novo = nomes.slice(1, 2); // fatia o array - inicio e fim - o indice final não é incluido

    // let novo = nome.split(' '); // adiciona string a um array e os separas
    // let nomeS = nome.join(' '); // uni todos os valores em uma string e qual o separador
}


let nome = ['Lucas', 'Gabriel', 'Gaiao'];

console.log(nomeS);